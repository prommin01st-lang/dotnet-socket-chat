meta {
  name: Get Messages
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/conversations/{{conversationId}}/messages
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  conversationId: 
}
