meta {
  name: Assign Role
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/users/{{userId}}/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "roleName": "Admin"
  }
}
