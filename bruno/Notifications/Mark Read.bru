meta {
  name: Mark Notification as Read
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/notifications/{{notificationId}}/read
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  notificationId: 
}
