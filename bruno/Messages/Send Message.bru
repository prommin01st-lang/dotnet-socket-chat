meta {
  name: Send Message
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/messages
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "conversationId": "{{conversationId}}",
    "content": "Hello World!"
  }
}

vars:pre-request {
  conversationId: 
}
